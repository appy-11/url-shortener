/**
 * Application configuration constants.
 * This configuration includes the application name and the domain used for short URLs.
 * These constants are used throughout the application for consistent references to the app's identity and URL structure.
 */
export const APP_CONFIG = {
  name: "Shortly",
  shortUrlDomain: "short.ly",
} as const;